(ns toy-chat.server.cli.main-test
  (:require [clojure.test :refer [deftest testing is]]
            [toy-chat.server.cli.main :as sut]))

(deftest a-test
  (testing "FIXME, I do nothing."
    (is (= 0 0))))
